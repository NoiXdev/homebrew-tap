# NoiX Homebrew tap

```bash
brew tap NoiXdev/tap
brew install <name>
```

One tap for every NoiX product: command-line tools in `Formula/`, GUI
applications in `Casks/`.

Adding one is a file, not a repository — see [SETUP.md](SETUP.md).
